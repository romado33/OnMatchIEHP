Implement full app improvement plan: legal compliance, structured profile, job postings, AI search hardening

Tier 1 - Critical (legal/safety):
- Add consent gate to _authenticated/route.tsx; new /consent page captures TOS, Privacy, and AI processing consent
- Make AppHeader role-aware: professionals see Browse Jobs, employers see Job Postings + Find Candidates
- Remove raw work_authorization from AI prompt, search results, and server function; surface only privacy-safe boolean
- Gate /search behind employer verification_status check with 4 gate states

Tier 2 - High value:
- Rewrite professional profile with tabbed layout, structured selects from reference tables, MultiSelectChips, TagsInput
- Profile completeness bar (0-100%) updates live; score stored on save
- CredentialTracker: per-step Ontario licensing pathway with status dropdowns, cost, duration, info links
- Dashboard: live stats for both roles (completeness, profile views, credential steps, verification banner, searches today)
- New /jobs page: employer CRUD with create/edit dialog; professional filterable read view with inline apply

Tier 3 - Polish:
- Redesign landing page with How It Works sections, trust pills, compliance section, legal footer links
- New /privacy (PIPEDA-compliant) and /terms static pages
- Search pre-filter panel (profession, min experience, work-authorized-only) and shortlist button on result cards
- Notification bell in AppHeader with unread count badge and mark-all-read popover
- Employer onboarding 3-step flow: collects org details and sets verification_status = under_review immediately

Migrations: reference lookup tables and feature/compliance tables added to supabase/migrations/
